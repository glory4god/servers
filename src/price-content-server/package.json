{
  "name": "@modelcontextprotocol/price-content-server",
  "version": "0.1.0",
  "description": "MCP server for API data fetching and processing",
  "license": "MIT",
  "type": "module",
  "bin": {
    "mcp-server-api": "dist/price-content-server.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "0.5.0",
    "zod-to-json-schema": "^3.23.5"
  },
  "devDependencies": {
    "@types/node": "^22",
    "shx": "^0.3.4",
    "typescript": "^5.3.3"
  }
}
